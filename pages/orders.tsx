import Head from 'next/head';import SiteHeader from '../components/SiteHeader';import OrderCenter from '../components/OrderCenter';export default function Orders(){return <><Head><title>Orders & Bookings | CulinaLinkTX</title></Head><SiteHeader/><main className='page'><div className='eyebrow'>Customer workspace</div><h1>Orders, bookings and fulfillment.</h1><p className='page-intro'>This view uses CulinaLinkTX records as the system of record and displays external delivery state only when the provider returned it.</p><OrderCenter/></main></>