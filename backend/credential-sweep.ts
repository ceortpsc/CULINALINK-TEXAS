import {db} from '@appdeploy/sdk';import {runVerificationSweep} from './verification';export async function runCredentialSweep(){const{items}=await db.list<{status:string;expiresAt?:string|null;expirationDate?:string|null}>('credential_registry',{limit:100});const n=Date.now();for(const item of items){const expiry=item.expiresAt||item.expirationDate;if(item.status==='VERIFIED'&&expiry&&new Date(expiry).getTime()<n){await db.update('credential_registry',[{id:item.id,record:{...item,status:'EXPIRED',expiredAt:new Date().toISOString()}}])}}await runVerificationSweep()}