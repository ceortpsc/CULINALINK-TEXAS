const{app,BrowserWindow,shell}=require('electron');const URL='https://culinalink-texas-mcczce.v2.appdeploy.ai/';function create(){const w=new BrowserWindow({width:1280,height:860,minWidth:900,minHeight:650,webPreferences:{contextIsolation:true,nodeIntegration:false,sandbox:true}});w.loadURL(URL);w.webContents.setWindowOpenHandler(({url})=>{shell.openExternal(url);return{action:'deny'}})}app.whenReady().then(()=>{create();app.on('activate',()=>{if(BrowserWindow.getAllWindows().length===0)create()})});app.on('window-all-closed',()=>{if(process.platform!=='darwin')app.quit()});