# CulinaLinkTX Desktop Packaging

Electron packaging scaffold for Windows and Linux. The wrapper loads the production CulinaLinkTX web client with context isolation, Node integration disabled and sandbox enabled.

Windows distribution still requires packaging, installer validation and recommended code signing. Linux distribution requires generated package validation for the target distribution. These files are packaging scaffolds, not proof of store publication or signed-binary release.
